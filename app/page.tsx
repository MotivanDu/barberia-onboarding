import LandingContent from './_components/LandingContent'

// Versão CLARA (clean) — página oficial. A versão escura fica em /escuro.
// Todo o conteúdo vive em _components/LandingContent (edite lá e vale nas duas).
export default function Home() {
  return <LandingContent theme="light" />
}
