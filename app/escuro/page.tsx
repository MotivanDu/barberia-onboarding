import LandingContent from '../_components/LandingContent'

// Versão ESCURA — para o teste A/B. Mesmo conteúdo da /, só muda o tema.
export default function HomeEscuro() {
  return <LandingContent theme="dark" />
}
